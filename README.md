# VA_Explorer

The VA Explorer (VADE) is a tool used to analyze verbal autopsy data at scale.
The development build can be downloaded from source and compiled by the following instructions.

Development Build:

[1] Run `npm install` from /my-app/ and /server/

[2] Run `npm run start:win` from /my-app/ (For Windows, replace with `:mac` for Mac)